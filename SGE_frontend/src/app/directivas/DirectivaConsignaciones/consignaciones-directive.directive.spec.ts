import { ConsignacionesDirectiveDirective } from './consignaciones-directive.directive';

describe('ConsignacionesDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ConsignacionesDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
