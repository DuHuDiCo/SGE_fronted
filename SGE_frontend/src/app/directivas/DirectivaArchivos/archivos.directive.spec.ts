import { ArchivosDirective } from './archivos.directive';

describe('ArchivosDirective', () => {
  it('should create an instance', () => {
    const directive = new ArchivosDirective();
    expect(directive).toBeTruthy();
  });
});
