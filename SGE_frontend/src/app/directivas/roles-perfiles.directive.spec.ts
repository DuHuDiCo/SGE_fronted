import { RolesPerfilesDirective } from './roles-perfiles.directive';

describe('RolesPerfilesDirective', () => {
  it('should create an instance', () => {
    const directive = new RolesPerfilesDirective();
    expect(directive).toBeTruthy();
  });
});
